{
  "name": "hanamal24-static",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "clean": "rimraf dist",
    "dev": "node scripts/build.js && npx serve dist -p 4321"
  },
  "dependencies": {
    "dotenv": "^17.2.2"
  },
  "devDependencies": {
    "marked": "12.0.2",
    "rimraf": "6.0.1"
  }
}
